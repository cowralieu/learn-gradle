# How to use this project

## build the project
Under the root path of the project, execute the following command:
```cmd
gradle build
```
After a while, you can execute:
```cmd
java -jar build/libs/todo-app-0.2.jar
```

That's all.